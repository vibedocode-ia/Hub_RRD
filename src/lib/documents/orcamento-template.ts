import { escapeHtml } from './escape-html'

export interface OrcamentoData { docNumber: string; issueDate: string; contractor: string; object: string; serviceScope: string; totalAmount: string; paymentMethod: string; validityDays: string; executionDeadline: string; guarantees: string }
const esc=(v:string)=>escapeHtml(String(v||''))
export function generateOrcamentoHTML(data: OrcamentoData): string {
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><title>Orçamento ${esc(data.docNumber)}</title><style>@page{size:A4;margin:0}*{box-sizing:border-box}html,body{margin:0;background:#fff;font-family:Arial,sans-serif}.page{position:relative;width:210mm;height:297mm;background-size:210mm 297mm;background-repeat:no-repeat;page-break-after:always}.field{position:absolute;color:#18263a;font-size:8pt;line-height:1.35;overflow:hidden}.white{background:rgba(255,255,255,.94);padding:2mm}.p1{background-image:url('/documents/templates/rr-orcamento-v1-page-1.png')}.p2{background-image:url('/documents/templates/rr-orcamento-v1-page-2.png')}.number{left:153mm;top:19mm;width:40mm;text-align:center;font-weight:bold;color:#fff;background:transparent}.date{left:153mm;top:29mm;width:40mm;text-align:center;font-weight:bold;color:#fff;background:transparent}.contractor{left:20mm;top:58mm;width:170mm;min-height:11mm}.object{left:20mm;top:78mm;width:170mm;min-height:11mm}.scope{left:20mm;top:108mm;width:170mm;height:71mm}.total{left:137mm;top:184mm;width:53mm;text-align:right;font-size:13pt;font-weight:bold;color:#063b70}.payment{left:20mm;top:202mm;width:170mm;min-height:10mm}.validity{left:20mm;top:232mm;width:170mm;min-height:10mm}.deadline{left:20mm;top:247mm;width:170mm;min-height:10mm}.guarantees{left:20mm;top:52mm;width:170mm;height:70mm}</style></head><body><main class="page p1"><div class="field number">${esc(data.docNumber)}</div><div class="field date">${esc(data.issueDate)}</div><div class="field white contractor">${esc(data.contractor)}</div><div class="field white object">${esc(data.object)}</div><div class="field white scope">${esc(data.serviceScope)}</div><div class="field total">R$ ${esc(data.totalAmount)}</div><div class="field white payment">${esc(data.paymentMethod)}</div><div class="field white validity">${esc(data.validityDays)}</div><div class="field white deadline">${esc(data.executionDeadline)}</div></main><main class="page p2"><div class="field white guarantees">${esc(data.guarantees)}</div></main></body></html>`
}
