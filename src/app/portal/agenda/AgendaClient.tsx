'use client'
import { useState } from 'react'
type Event={id:string,title:string,startsAt:string|Date,endsAt:string|Date,googleSyncStatus:string,teamId?:string|null,vehicleId?:string|null}
type Option={id:string,name:string}
export default function AgendaClient({initial,contacts,clients,teams,vehicles}:{initial:Event[];contacts:Option[];clients:Option[];teams:Option[];vehicles:Option[]}){const [events,setEvents]=useState(initial),[title,setTitle]=useState(''),[start,setStart]=useState(''),[end,setEnd]=useState(''),[contactId,setContact]=useState(''),[clientId,setClient]=useState(''),[teamId,setTeam]=useState(''),[vehicleId,setVehicle]=useState(''),[message,setMessage]=useState('');async function add(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/agenda',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,startsAt:new Date(start).toISOString(),endsAt:new Date(end).toISOString(),contactId:contactId||null,clientId:clientId||null,teamId:teamId||null,vehicleId:vehicleId||null})});if(!res.ok){setMessage('Não foi possível salvar o evento.');return}const data=await res.json();setEvents(x=>[...x,data.event]);setMessage('Evento salvo com equipe e veículo vinculados.');setTitle('');setStart('');setEnd('')}return <div className="space-y-5"><form onSubmit={add} className="grid gap-3 rounded-2xl border border-slate-800 bg-slate-900/60 p-4 md:grid-cols-3"><input required value={title} onChange={e=>setTitle(e.target.value)} placeholder="Título do compromisso" className="rounded-lg bg-slate-950 p-2"/><input required type="datetime-local" value={start} onChange={e=>setStart(e.target.value)} className="rounded-lg bg-slate-950 p-2"/><input required type="datetime-local" value={end} onChange={e=>setEnd(e.target.value)} className="rounded-lg bg-slate-950 p-2"/><select value={teamId} onChange={e=>setTeam(e.target.value)} className="rounded-lg bg-slate-950 p-2"><option value="">Sem equipe</option>{teams.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={vehicleId} onChange={e=>setVehicle(e.target.value)} className="rounded-lg bg-slate-950 p-2"><option value="">Sem veículo</option>{vehicles.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={contactId} onChange={e=>setContact(e.target.value)} className="rounded-lg bg-slate-950 p-2"><option value="">Sem contato</option>{contacts.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select value={clientId} onChange={e=>setClient(e.target.value)} className="rounded-lg bg-slate-950 p-2"><option value="">Sem empresa</option>{clients.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><button className="rounded-lg bg-cyan-600 p-2 font-bold text-white">Adicionar evento</button></form>{message&&<p className="text-sm text-cyan-400">{message}</p>}<div className="space-y-3">{events.map(event=><article key={event.id} className="rounded-xl border border-slate-800 bg-slate-900/60 p-4"><h3 className="font-bold text-white">{event.title}</h3><p className="text-xs text-slate-400">{new Date(event.startsAt).toLocaleString('pt-BR')} → {new Date(event.endsAt).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</p><p className="mt-2 text-xs text-cyan-400">Equipe: {teams.find(x=>x.id===event.teamId)?.name||'Sem vínculo'} · Veículo: {vehicles.find(x=>x.id===event.vehicleId)?.name||'Sem vínculo'}</p></article>)}</div></div>}
