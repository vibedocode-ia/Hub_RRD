'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
type Contact={id:string,name:string,phone:string|null,email:string|null,title:string|null,photoUrl:string|null,clientId:string|null}
export default function ContactsClient({initial,companies}:{initial:Contact[],companies:{id:string,name:string}[]}){const r=useRouter(),[items,setItems]=useState(initial),[name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[title,setTitle]=useState(''),[clientId,setClient]=useState(''),[message,setMessage]=useState('');async function add(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/contatos',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,phone:phone||null,email:email||null,title:title||null,clientId:clientId||null})});if(!res.ok){setMessage('Não foi possível salvar o contato.');return}const data=await res.json();setItems(x=>[...x,data.contact]);setName('');setPhone('');setEmail('');setTitle('');setClient('');setMessage('Contato salvo.')}return <div className="space-y-5"><form onSubmit={add} className="grid gap-3 rounded-2xl border border-slate-800 bg-slate-900/60 p-4 md:grid-cols-3"><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Nome da pessoa" className="rounded-lg bg-slate-950 p-2"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Telefone" className="rounded-lg bg-slate-950 p-2"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail" className="rounded-lg bg-slate-950 p-2"/><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Cargo" className="rounded-lg bg-slate-950 p-2"/><select value={clientId} onChange={e=>setClient(e.target.value)} className="rounded-lg bg-slate-950 p-2"><option value="">Sem empresa vinculada</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button className="rounded-lg bg-cyan-500 p-2 font-bold text-slate-950">Adicionar contato</button></form>{message&&<p className="text-sm text-cyan-300">{message}</p>}<div className="grid gap-3 md:grid-cols-2">{items.map(c=><article key={c.id} className="flex gap-3 rounded-xl border border-slate-800 bg-slate-900/50 p-4">{c.photoUrl?<img src={c.photoUrl} className="h-10 w-10 rounded-full object-cover" alt=""/>:<div className="flex h-10 w-10 items-center justify-center rounded-full bg-cyan-700 font-bold">{c.name[0]}</div>}<div><b>{c.name}</b><p className="text-sm text-slate-400">{c.title||'Contato'} · {c.phone||c.email||'sem telefone/e-mail'}</p></div></article>)}</div></div>}