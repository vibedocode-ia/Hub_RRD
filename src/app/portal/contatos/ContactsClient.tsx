'use client'
import { useState } from 'react'
type Contact={id:string,name:string,phone:string|null,email:string|null,title:string|null,photoUrl:string|null,linkedinUrl:string|null,instagramUrl:string|null,websiteUrl:string|null,clientId:string|null}
export default function ContactsClient({initial,companies}:{initial:Contact[],companies:{id:string,name:string}[]}){const [items,setItems]=useState(initial),[form,setForm]=useState({name:'',phone:'',email:'',title:'',photoUrl:'',linkedinUrl:'',instagramUrl:'',websiteUrl:'',clientId:''}),[message,setMessage]=useState('');const set=(k:string,v:string)=>setForm(x=>({...x,[k]:v}));async function add(e:React.FormEvent){e.preventDefault();const payload=Object.fromEntries(Object.entries(form).map(([k,v])=>[k,v||null]));const res=await fetch('/api/contatos',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});if(!res.ok){setMessage('Não foi possível salvar o contato.');return}const data=await res.json();setItems(x=>[...x,data.contact]);setForm({name:'',phone:'',email:'',title:'',photoUrl:'',linkedinUrl:'',instagramUrl:'',websiteUrl:'',clientId:''});setMessage('Contato salvo.')}return <div className="space-y-5"><form onSubmit={add} className="grid gap-3 rounded-2xl border border-slate-800 bg-slate-900/60 p-4 md:grid-cols-3"><input required value={form.name} onChange={e=>set('name',e.target.value)} placeholder="Nome da pessoa" className="rounded-lg bg-slate-950 p-2"/><input value={form.phone} onChange={e=>set('phone',e.target.value)} placeholder="Telefone" className="rounded-lg bg-slate-950 p-2"/><input type="email" value={form.email} onChange={e=>set('email',e.target.value)} placeholder="E-mail" className="rounded-lg bg-slate-950 p-2"/><input value={form.title} onChange={e=>set('title',e.target.value)} placeholder="Cargo" className="rounded-lg bg-slate-950 p-2"/><input type="url" value={form.photoUrl} onChange={e=>set('photoUrl',e.target.value)} placeholder="URL da foto" className="rounded-lg bg-slate-950 p-2"/><select value={form.clientId} onChange={e=>set('clientId',e.target.value)} className="rounded-lg bg-slate-950 p-2"><option value="">Sem empresa vinculada</option>{companies.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input type="url" value={form.linkedinUrl} onChange={e=>set('linkedinUrl',e.target.value)} placeholder="LinkedIn" className="rounded-lg bg-slate-950 p-2"/><input type="url" value={form.instagramUrl} onChange={e=>set('instagramUrl',e.target.value)} placeholder="Instagram" className="rounded-lg bg-slate-950 p-2"/><input type="url" value={form.websiteUrl} onChange={e=>set('websiteUrl',e.target.value)} placeholder="Site" className="rounded-lg bg-slate-950 p-2"/><button className="rounded-lg bg-cyan-500 p-2 font-bold text-slate-950">Adicionar contato</button></form>{message&&<p className="text-sm text-cyan-300">{message}</p>}<div className="grid gap-3 md:grid-cols-2">{items.map(c=><article key={c.id} className="flex gap-3 rounded-xl border border-slate-800 bg-slate-900/50 p-4">{c.photoUrl?<img src={c.photoUrl} className="h-10 w-10 rounded-full object-cover" alt=""/>:<div className="flex h-10 w-10 items-center justify-center rounded-full bg-cyan-700 font-bold">{c.name[0]}</div>}<div><b>{c.name}</b><p className="text-sm text-slate-400">{c.title||'Contato'} · {c.phone||c.email||'sem telefone/e-mail'}</p><p className="mt-1 flex gap-2 text-xs text-cyan-300">{c.linkedinUrl&&<a href={c.linkedinUrl} target="_blank">LinkedIn</a>}{c.instagramUrl&&<a href={c.instagramUrl} target="_blank">Instagram</a>}{c.websiteUrl&&<a href={c.websiteUrl} target="_blank">Site</a>}</p></div></article>)}</div></div>}