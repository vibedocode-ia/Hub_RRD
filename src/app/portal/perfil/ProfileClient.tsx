'use client'
import { useState } from 'react'
export default function ProfileClient({profile}:{profile:{name:string,email:string|null,jobTitle:string|null,photoUrl:string|null}}){const [form,setForm]=useState(profile),[message,setMessage]=useState('');async function save(e:React.FormEvent){e.preventDefault();const res=await fetch('/api/profile',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(form)});setMessage(res.ok?'Perfil atualizado.':'Não foi possível atualizar o perfil.')}return <form onSubmit={save} className="grid max-w-xl gap-4 rounded-2xl border border-slate-800 bg-slate-900/60 p-5"><h1 className="text-2xl font-black">Meu perfil</h1><p className="text-sm text-slate-400">Esses dados aparecem no topo do Hub RRD.</p><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Nome" className="rounded-lg bg-slate-950 p-2"/><input value={form.jobTitle||''} onChange={e=>setForm({...form,jobTitle:e.target.value||null})} placeholder="Cargo" className="rounded-lg bg-slate-950 p-2"/><input value={form.email||''} onChange={e=>setForm({...form,email:e.target.value||null})} placeholder="E-mail" className="rounded-lg bg-slate-950 p-2"/><input value={form.photoUrl||''} onChange={e=>setForm({...form,photoUrl:e.target.value||null})} placeholder="URL segura da foto" className="rounded-lg bg-slate-950 p-2"/>{form.photoUrl&&<img src={form.photoUrl} alt="Prévia" className="h-20 w-20 rounded-full object-cover"/>}<button className="rounded-lg bg-cyan-500 p-2 font-bold text-slate-950">Salvar perfil</button>{message&&<p className="text-sm text-cyan-300">{message}</p>}</form>}